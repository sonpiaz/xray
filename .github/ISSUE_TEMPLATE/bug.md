---
name: Bug report
about: Something broke when running XRay
title: 'bug: '
labels: ['bug']
---

**Steps to reproduce**
1.
2.
3.

**Expected behavior**

**Actual behavior**

**Environment**
- XRay version: (run `xray --version`)
- OS:
- Bun version: (run `bun --version`)
- Chromium installed via Playwright? (yes / no)
- `xray cache info` output:

**Logs**

Re-run with `XRAY_LOG_LEVEL=debug` and paste the relevant lines.

**Cache state (if relevant)**
- Did the bug appear on the first run, or only after some cached state existed?
